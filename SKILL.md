---
name: local-research
description: 调研 GitHub 项目、抖音内容与 X 收藏，生成本地 Markdown 文档并按主题和来源分文件夹归档。用于功能介绍、首次初始化、环境检查、项目评估、链接分析、收藏或 Star 定时增量、抖音选题搜索及跨来源综述；适用于希望结果保存在本地文档的任务，不负责向外部知识库发布。
---

# 本地情报调研

把来源材料转成可追溯的分析与本地文档。此技能独立工作，不依赖其他 Skill、特定浏览器插件、私有脚本或知识库服务。

## 首次使用与准备检查

首次裸调用或用户询问“有哪些功能 / 怎么开始”时，先按 [初始化与准备](references/onboarding.md) 展示简短功能导览：GitHub、抖音、X、本地资料综述、定时调研，以及本地分类文档产物，再询问要从哪项开始。功能咨询不触发安装、账号访问或文件写入。已有具体任务时跳过菜单，直接处理。

用户要求“初始化 / 开始使用 / 检查环境”，首次执行调研，或新增平台、切换宿主与账号时，先读 [初始化与准备](references/onboarding.md)。只检查本次所需能力；已有任务信息直接复用，不让用户重新填写问卷。准备已完成则进入调研，缺失时给出具体安装或登录步骤与成功判据，不能只说“请配置工具”。

抖音主题搜索、推荐流筛选先取得用户指定或已保存的关注主题；缺少时先问用途和主题。用户明确选择不限主题探索时，推荐流可按探索模式执行，无需强制指定主题。指定链接、转写及收藏整理不强制询问主题，按内容归类；主题过滤必须是用户要求的，不能擅自丢弃其他收藏。详见 [抖音流程](references/douyin.md)。

## 输出与范围

- 使用本技能执行调研、链接分析、收藏整理或综述时，默认写本地 Markdown。用户明确要求仅口头解释、只读审阅或不保存时，在对话内交付。
- 归档根目录优先使用用户本次指定位置，否则读取环境变量 `LOCAL_RESEARCH_ROOT`，未设置时使用当前用户主目录下的 `Documents/Research/`。路径展开后转成绝对路径；无法确定主目录或无写入权限时询问可用目录，不静默换盘。先读取 [本地归档](references/archive.md)，按主题、来源和条目保存并更新索引。
- 只采集请求只保存来源材料与采集状态，不自动扩展为完整分析。指定链接限于指定对象，外链不自动扩大研究范围。
- 只写选定归档根目录，不调用外部入库、队列或看板刷新。用户提供的既有资料可只读参考，不改写来源档案。
- 不迁移历史资料、不取消平台收藏、不点赞关注或发帖。仅在用户本次明确要求时配置定时任务；不从文件或历史示例中推定授权。

首次在线接入及首次启用定时任务前，按 [初始化流程](references/onboarding.md) 展示平台自动化访问风险提醒；验证码、限流或账号警告应暂停受影响来源并记录状态，不持续重试或绕过限制。

## 运行能力

需要能读写本地文件的 Agent；联网研究还需要网页读取、搜索或相应平台的只读 API。GitHub CLI 是可选途径，浏览器不限定品牌或插件。只使用当前环境实际提供的工具并读取其文档，不凭技能文本假定命令存在。

没有联网或浏览器能力时仍可分析用户提供的文件、正文和转写；标明无法核实当前状态。没有下载或转写工具时保存已获得内容及缺口，不声称视频已读完。此包不附带下载器、转写引擎或自动调度器，需要安装时按初始化流程提供针对当前环境的指导；用户已授权安装则完成相应安装和验证，不把普通初始化当作批量安装授权。

## 按任务读取

| 任务 | 参考 |
| --- | --- |
| GitHub 评估或比较 | [GitHub 调研](references/github.md) |
| 抖音收藏、指定内容、推荐流或主题搜索 | [抖音采集与分析](references/douyin.md) |
| X 收藏或指定帖子 | [X 分析](references/x.md) |
| Star / 收藏增量、恢复任务 | [本地增量](references/incremental.md) |
| 已有资料综述、跨来源比较 | [资料综述](references/digests.md) |

## 证据与完成

读取实际原文、代码、文档或转写。区分作者主张、已查证事实与分析判断；摘要、卡片和未转写视频不能代表完整内容。变化性事实带查询时间和直接来源链接，缺关键材料时写明限制。

完成前确认报告非空、来源身份一致、证据路径和索引链接有效、状态符合实际。交付主要结论、可点击的绝对文档路径与必要缺口；区分已采集、部分分析和完整分析。无需启动任何本地服务。
